# Core dependencies for Streamlit version
# Note: pygame is required for the desktop version and includes SDL2 bundled in pre-built wheels
# For platforms requiring system SDL2 libraries, see packages.txt
# pygame 2.6.1+ adds Python 3.13 support and fixes wheel build issues
pygame==2.6.1
streamlit>=1.28.0
Pillow>=10.0.0
